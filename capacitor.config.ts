import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nationalcompany.myedu',
  appName: 'MyEdu',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
